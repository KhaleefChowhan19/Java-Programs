package timeconverter;

public class TimeConverter {
public static double convertToHours(double time)
{
	double hour=time/60;
	System.out.println("the time converter minutes in to hours is"+hour);
	return hour;
}
}
