Write a java Program to create an integer array by taking length as input 
from the user. Take the contents of the array from user and store the contents
in the array.Display the contents of the array on to the console.



Explanation:

first we can create the scanner class from asking the user input after we can
create the size of array and store it.
we can create the one dimensional int data type array will be created.
After we can store the elements in array by using for loop.
after displaying the elements present in array.

