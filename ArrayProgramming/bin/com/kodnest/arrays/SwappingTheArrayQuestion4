Write a java program to take array length as input from user and create an
 integer array and store elements in to the array by taking elements as input
 from keyboard.Before Swaping print the contents of array Ask user to enter 2
 indexes.Swap the values of 2 indexes After Swapping print the contents of
 array.
 
 Sample input output:
 Before Swapping:Arr-->10 20 30 40 50
 After Swapping:Arr-->10 30 20 40 50
 
 
 Explanation:
 First we can create the size of array from asking the user input.
 after we can create the int type 1D array.
 after we can store the elements by usig for loop.
 after we can print the before swapping the array elements.
 after we can create the if statement in which Swap the values at specified
 index.
 after we can create the index1 and index2 variable from asking the 
 user input.
 after we can create the extra one variable like int temp.This variable will
 be assign both index1 and index2.
 after print the after swaping the values presented in array by useing
 for each loop.
