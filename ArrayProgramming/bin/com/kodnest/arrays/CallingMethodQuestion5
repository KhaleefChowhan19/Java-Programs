Write a java program to take array length as input from user and create an 
integer array Take elements from user and store elemets in to the array call
a method printArray()present in print array class which wil print the array
in forward direction and in reverse direction.
Note:Use each loop for printing in forward direction.


Explanation:

first we can create the scanner class from asking the user input after we can
create the size of array and store it.
we can create the one dimensional int data type array will be created.
After we can store the elements in array by using for loop.
After we can create the calling method.
create a another class and which we can print the array elements by using
for each loop.